name: Azure Static Web Apps CI/CD (disabled - using App Service)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Static Web Apps workflow disabled; deploy via Azure App Service instead."
